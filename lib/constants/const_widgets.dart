import 'package:flutter/material.dart';

import 'package:flutter_spinkit/flutter_spinkit.dart';

const rotatingCircle = SpinKitRotatingCircle(
  color: Colors.green,
  size: 50.0,
);

const fadingCircle = SpinKitFadingCircle(
  color: Colors.green,
  size: 50.0,
);

const cubeGrid = SpinKitCubeGrid(
  color: Colors.green,
  size: 50.0,
);

const fadingFour = SpinKitFadingFour(
  color: Colors.green,
  size: 50.0,
);

const dualRing = SpinKitDualRing(
  color: Colors.green,
  size: 50.0,
);

const dancingSquare = SpinKitDancingSquare(
  color: Colors.green,
  size: 50.0,
);
